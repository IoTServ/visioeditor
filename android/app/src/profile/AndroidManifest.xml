<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Merged on top of src/main. INTERNET is already declared in main for
         Agent live preview; kept here so Flutter tooling remains explicit for
         profile builds. -->
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>
